" Loader. Real config lives in ~/.vim/vimrc.d/, sourced in numeric order.
" Order is load-bearing and s: names are file-scoped -- see ~/.vim/CLAUDE.md.

for s:file in sort(glob('~/.vim/vimrc.d/*.vim', 0, 1))
  execute 'source' fnameescape(s:file)
endfor
